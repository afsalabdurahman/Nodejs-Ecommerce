<link rel="stylesheet" href="/assets/UserProfile/Cart.css">

<div class="body">
    <div class="container">
        <h2>MY CART</h2>
        <table>
            <thead>
                <tr>
                    <th></th>
                    <th>Product</th>
                    <th>Price</th>
                    <td>DiscountRate</td>
                    <th>Quantity</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>

                {{#each Datas}} <tr>
                    <td>
                        <a href="/profile/deletecart?id={{ProductId._id}}&quandity={{quandity}}"
                            class="remove-btn">X</a>
                        <img src="/upload/{{ProductId.image.[0]}}" alt="Product Image" class="product-image">
                    </td>
                    <td>
                        <p id="productId" style="display: none;">{{ProductId._id}}</p>
                        {{ProductId.name}}<br>
                        <span class="size">{{size}}</span>
                    </td>
                    <td>₹{{ProductId.price}}

                    </td>
                    {{#if offer}}
                    <td>
                        <p id="discountprice"> {{offer }}%</p>
                    </td>
                    {{else}}
                    <td>
                        <p>NA</p>
                    </td>
                    {{/if}}
                    <td>

                        <input type="text" style=" pointer-events: none;" class="quantity-input" value="{{quandity}}"
                            readonly>

                    </td>
                    <td class="item-total">₹{{TotalAmount}}</td>
                </tr>

                {{/each}}
            </tbody>
        </table>
        <div class="summary">
            <div class="coupon">
                <input type="text" placeholder="Enter coupon code" id="coupon-code">
                <button class="apply-btn" onclick="applyDiscount()">Apply</button>
            </div>
            <p>Subtotal: <span id="subtotal">₹{{totalPrice}}</span></p>
            <p>Discount: <span id="discount">0.00</span></p>
            <p>Total: <span id="total">₹{{totalPrice}}</span></p>
            <button class="proceed-btn" onclick="checkout()">Proceed to Checkout</button>
        </div>
    </div>
</div>

{{!--
<script>
    function ajx(quandity, id, productId) {
        console.log(quandity, "qunadity", id, "product")
        let xhttp = new XMLHttpRequest();
        xhttp.onreadystatechange = function () {
            if (this.readyState === 4 && this.status === 200) {
                // Handle the response if needed
                console.log(this.responseText);
            }
        };
        xhttp.open("GET", `/profile/newcart?id=${id}&newqunadity=${quandity}&productId=${productId}`, true);
        xhttp.send();
    }




    func { {applyDiscount ProductId.price ProductId.discount_price } }
    }

    function updateSummary(discount = 0) {
        const rows = document.querySelectorAll('tbody tr');
        let subtotal = 0;
        rows.forEach(row => {
            const itemTotal = parseFloat(row.querySelector('.item-total').textContent.replace('₹', ''));
            subtotal += itemTotal;
        });
        const discountAmount = subtotal * discount;
        const total = subtotal - discountAmount;

        document.getElementById('subtotal').textContent = `₹${subtotal.toFixed(2)}`;
        document.getElementById('discount').textContent = `₹${discountAmount.toFixed(2)}`;
        document.getElementById('total').textContent = `₹${total.toFixed(2)}`;
    }

    function checkout() {
        const total = document.getElementById('total').textContent.replace('₹', '');
        console.log(total, "totalssssss")
        window.location.href = `/profile/checkout?total=${total}`;
    }
</script> --}}
<script>
    function ajx(quantity, id, productId) {
        console.log(quantity, "quantity", id, "product");
        let xhttp = new XMLHttpRequest();
        xhttp.onreadystatechange = function () {
            if (this.readyState === 4 && this.status === 200) {
                // Handle the response if needed
                console.log(this.responseText);
            }
        };
        xhttp.open("GET", `/profile/newcart?id=${id}&newquantity=${quantity}&productId=${productId}`, true);
        xhttp.send();
    }

    function updateSummary(discount = 0) {
        const rows = document.querySelectorAll('tbody tr');
        let subtotal = 0;
        rows.forEach(row => {
            const itemTotal = parseFloat(row.querySelector('.item-total').textContent.replace('₹', ''));
            subtotal += itemTotal;
        });
        const discountAmount = subtotal * discount;
        const total = subtotal - discountAmount;

        document.getElementById('subtotal').textContent = `₹${subtotal.toFixed(2)}`;
        document.getElementById('discount').textContent = `₹${discountAmount.toFixed(2)}`;
        document.getElementById('total').textContent = `₹${total.toFixed(2)}`;
    }

    function checkout() {
        const total = document.getElementById('total').textContent.replace('₹', '');
        console.log(total, "totalssssss");
        window.location.href = `/profile/checkout?total=${total}`;
    }
</script>